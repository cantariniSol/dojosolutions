global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: dojosolutions
    metrics_path: /metrics
    static_configs:
      - targets:
          - app:3000

  - job_name: prometheus
    static_configs:
      - targets:
          - localhost:9090
