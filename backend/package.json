{
  "name": "dojosolutions-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Express API and metrics server for DojoSolutions.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "node --check server.js && find src -name '*.js' -print0 | xargs -0 -n1 node --check",
    "test": "npm run test:unit && npm run test:integration",
    "test:unit": "node --test test/unit/**/*.test.js",
    "test:integration": "node --test test/integration/**/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "5.2.1",
    "mongoose": "9.10.1",
    "prom-client": "15.1.3"
  },
  "devDependencies": {
    "supertest": "7.2.2"
  }
}
